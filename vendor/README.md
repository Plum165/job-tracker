# Vendor Dependencies

This folder is reserved for third-party libraries, fallback polyfills, or non-npm embedded static dependencies.

Currently, dependencies are managed natively via `package.json` and standard Vite bundling.
